export enum VerificationType {
    Identity = "identity",
    Academic = "academic",
    License = "license",
}

export enum VerificationStatus {
    Pending = "pending",
    InReview = "in_review",
    ActionRequired = "action_required",
    Approved = "approved",
    Rejected = "rejected",
}